import React from 'react';
import { View, Text } from 'react-native'; 

const ParentDashboard = () => {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Панель родителя</Text>
      </View>
    );
  };
  
  export default ParentDashboard;